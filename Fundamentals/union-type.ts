let userId: number | string;

userId = 3;
console.log(userId);

userId= 'Max';
console.log(userId);

// Atention to use brackets before array
let customerIds: (number | string) [] = [];
customerIds.push(88);
customerIds.push('146');
